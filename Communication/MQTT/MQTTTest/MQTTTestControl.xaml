<UserControl x:Class="AutomationControls.Communication.MQTT.MQTTTestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:convert="clr-namespace:AutomationControls.WPF.Converters;assembly=PD_ClassLibrary"
             xmlns:comm="clr-namespace:AutomationControls.Communication"
             xmlns:local="clr-namespace:AutomationControls.Communication.MQTT">
    <DockPanel>
        <!--<local:MQTTBrokerControl x:Name="ucBroker" DataContext="{Binding broker}"/>
        <local:MQTTClientControl x:Name="ucClient" DataContext="{Binding client}"/>-->
        <comm:CommunicationMonitor x:Name="ucMonitor" />
    </DockPanel>
</UserControl>