<UserControl x:Class="AutomationControls.Communication.Pipes.PipeServerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>                  
        <CheckBox x:Name="cbMonitor" Content="Monitor" Checked="cbMonitor_Checked" Unchecked="cbMonitor_Unchecked"   />
        <Button x:Name="btnStartServer" Content="Open Pipe" Click="btnStartServer_Click" />
        <TextBox x:Name="tbPipeName" Text="Codex" />
        <TextBlock x:Name="txbStatus" Text="{Binding log}" />
    </DockPanel>
</UserControl>
